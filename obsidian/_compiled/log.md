# Compile Log

Append-only record of `compile_kb.py` preprocessor runs and Claude Opus compile sessions.
Newest entries at the top.

Format: `YYYY-MM-DD HH:MM — <action> — <details>`

---

(no entries yet)
